
bool has_cuda() {
    // int count = 0;

    // CUresult res = cuInit(0);

    // if (res != CUDA_SUCCESS)
    //     return false;

    // res = cuDeviceGetCount(&count);

    // if (res != CUDA_SUCCESS)
    //     return false;

    // return count > 0;
    return false;
}
